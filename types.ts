export interface NavLink {
  label: string;
  href: string;
}

export interface Partner {
  name: string;
  logoText: string;
}

export interface TalentProfile {
  id: string;
  name: string;
  role: string;
  imageUrl: string;
}